PASOS GIT Y GITHUB — Compras TN UIO con IA
Generado el 12 de agosto de 2026

Rama oficial: main
Variantes: codex/version-webapp-3.0, codex/integracion-gmail-4.3.3
Repositorio previsto: https://github.com/eslay07/compras-tn-uio-ai-platform

1. ABRIR EL PROYECTO Y REVISAR

Set-Location 'F:\Proyectos 2026\compras-tn-uio-ai-platform'
git status --short
git log --oneline --decorate --graph --all -n 30
git branch --all
git tag --list

2. VALIDAR ANTES DE PUBLICAR

Get-Content -Raw .\Remoto.js | node --check -


3. REEMPLAZAR UNA SOLA VEZ LA RAMA REMOTA SANEADA

ADVERTENCIA: el historial original ya está respaldado en ..\_respaldos_git. Revisa GitHub antes de ejecutar. No publiques ramas de respaldo ni referencias antiguas.

git remote -v
$expected = (git ls-remote origin refs/heads/main).Split()[0]
if (-not $expected) { throw 'No se encontró la rama remota esperada' }
git push --force-with-lease=main:$expected origin main:main

# Las variantes nuevas se publican normalmente:
git push -u origin codex/version-webapp-3.0
git push -u origin codex/integracion-gmail-4.3.3
git push origin --tags

# Después de verificar GitHub, elimina manualmente ramas remotas antiguas que contengan datos sensibles.
# Primero lista: git ls-remote --heads origin
# Luego, una por una: git push origin --delete NOMBRE_RAMA_ANTIGUA

# Los siguientes cambios ya usan push normal:
# git push origin main

4. FLUJO PARA FUTUROS CAMBIOS

git switch main
git pull --ff-only origin main
git status --short

# Edita y prueba tus cambios. Después revisa exactamente lo que vas a confirmar:
git diff
git add -A
git diff --cached --stat
git diff --cached
git commit -m "feat: mejorar clasificación asistida de compras" -m "Describe aquí qué cambió, por qué y cómo se validó."
git push origin main

5. CREAR UNA RAMA PARA UNA VERSIÓN ALTERNATIVA

git switch main
git pull --ff-only origin main
git switch -c codex/nombre-de-la-variante
# Realiza y valida los cambios.
git add -A
git commit -m "feat: agregar variante nombre-de-la-variante"
git push -u origin codex/nombre-de-la-variante

6. COMPROBACIONES DE SEGURIDAD

git status --short
git ls-files | Select-String -Pattern '(^|/)(\.env|\.clasp\.json|config\.json)$|\.(db|sqlite|log|key|pem|p12|pfx|keystore|jks)$'

Si aparece una credencial, no hagas push. Retírala, revócala en el proveedor y vuelve a revisar todo el historial. Nunca uses fechas falsas: cada commit debe reflejar trabajo real ejecutado ese día.
